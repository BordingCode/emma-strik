// Beregner — knitting calculators. Built in Milestone 2.
export function initCalculators(node) {
  node.innerHTML = `<div class="pagehead"><h1>Beregner</h1>
    <p class="hint">Strikkeberegnere er på vej: fordel ind-/udtagninger jævnt, omregn ved anden
    strikkefasthed, og beregn garnmængde &amp; -erstatning.</p></div>
    <p class="empty">🧮 Kommer i næste opdatering.</p>`;
}
