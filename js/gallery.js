// Opskrifter — curated pattern gallery (DK + EN, well-recommended). Built in Milestone 3.
export function initGallery(node) {
  node.innerHTML = `<div class="pagehead"><h1>Opskrifter</h1>
    <p class="hint">En håndplukket samling af rigtig gode strikkeopskrifter (dansk og engelsk),
    med link til den originale opskrift.</p></div>
    <p class="empty">🧶 Kommer i næste opdatering.</p>`;
}
